FROM node:20-alpine

WORKDIR /app

# Install dependencies first (layer cache)
COPY package*.json ./
RUN npm ci

# Source is mounted via volume at runtime — no COPY needed
EXPOSE 3000

# next dev with polling so inotify works inside Docker on all OSes
CMD ["npm", "run", "dev"]
